# fib_filewatch

Haxe/fiberus @:native binding for filewatching

### Setup

```bash
haxelib git fib_filewatch https://github.com/fiberus-hx/fib_filewatch.git
```

### folders
`src` - contains the haxe externs
`lib` - contains the native code + dependencies